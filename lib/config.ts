// ─── Room render mode flag ───────────────────────────────────────────────────
// "fake" → Fake.glb  (procedural room built from wall/floor/ceiling/window textures)
// "real" → Hall.glb  (original 3D scanned hall)
export const ROOM_MODE: "fake" | "real" = "real"
